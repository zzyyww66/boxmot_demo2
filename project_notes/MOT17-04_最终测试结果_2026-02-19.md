# MOT17-04 最终测试结果（2026-02-19）

## 1. 测试信息

- 测试时间：2026-02-19 15:01（本地）
- 执行目录：`/home/zyw/code/boxmot_demo2`
- 执行命令：

```bash
uv run python run_improved_only.py
```

- 数据源：`runs/ablation/_mot17_04_subset`（MOT17-04）
- 结果目录：`runs/ablation/mot/yolox_x_MOT17_ablation_lmbn_n_duke_bytetrack_104`

---

## 2. 本次运行使用的改进配置

来自 `run_improved_only.py`：

```python
IMPROVED_CONFIG = {
    "new_track_thresh": 0.65,
    "entry_margin": 50,
    "strict_entry_gate": False,
    "birth_confirm_frames": 2,
    "birth_suppress_iou": 0.7,
    "birth_suppress_center_dist": 35,
    "zombie_max_history": 100,
    "zombie_dist_thresh": 150,
    "zombie_max_predict_frames": 5,
    "zombie_transition_frames": 30,
    "zombie_match_max_dist": 200,
    "lost_max_history": 0,
    "exit_zone_enabled": True,
    "exit_zone_margin": 50,
    "exit_zone_remove_grace": 30,
    "adaptive_zone_enabled": True,
    "adaptive_zone_update_mode": "always_expand",
    "adaptive_zone_expand_trigger": "all_high",
    "adaptive_zone_min_box_area": 0,
}
```

---

## 3. 关键指标（MOT17-04）

来源：

- 控制台 summary（四舍五入显示）
- `person_detailed.csv`（精确值）

### 3.1 主指标

- `HOTA`: `78.31%`（精确 `0.7830450686`）
- `MOTA`: `89.90%`（精确 `0.8990404500`）
- `IDF1`: `90.48%`（精确 `0.9048515266`）
- `IDSW`: `19`
- `IDs`: `73`

### 3.2 关联与轨迹补充指标

- `AssA`: `78.71%`（`AssA___AUC=0.7871407452`）
- `AssRe`: `83.89%`（`AssRe___AUC=0.8389302702`）
- `Fragments`: `117`
- `MT`: `60`
- `PT`: `7`
- `ML`: `2`

---

## 4. 运行性能与备注

- Tracking 时延：`4038.7 ms / 524 帧`，平均 `7.71 ms/frame`（约 `129.7 FPS`，仅关联阶段）
- 检测与特征为缓存命中（日志：`Skipping MOT17-04 (cached complete; 524/524 frames)`）
- 脚本执行结束后已自动恢复 `boxmot/configs/trackers/bytetrack.yaml`

---

## 5. 结果文件定位

- 汇总：`runs/ablation/mot/yolox_x_MOT17_ablation_lmbn_n_duke_bytetrack_104/person_summary.txt`
- 明细：`runs/ablation/mot/yolox_x_MOT17_ablation_lmbn_n_duke_bytetrack_104/person_detailed.csv`
- 轨迹：`runs/ablation/mot/yolox_x_MOT17_ablation_lmbn_n_duke_bytetrack_104/MOT17-04.txt`
